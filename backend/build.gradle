group 'tech.l-o-w'
version '1.0-SNAPSHOT'

apply plugin: 'java'

sourceCompatibility = 1.8

repositories {
    mavenCentral()
}

dependencies {
    compile "com.sparkjava:spark-core:2.6.0"
    compile "com.stripe:stripe-java:5.8.0"
    testCompile group: 'junit', name: 'junit', version: '4.12'
}
